/**
 * External Dependencies
 */
import React from 'react';

/**
 * A simple alert dialog that works with `PopupOverlay` component.
 * 
 * @param {object} props 
 */
const Alert = (props) => {
  return (
    <div className="alert">
    </div>
  );
};

export default Alert;
