repositories {
    jcenter()
}

dependencies {
    implementation(project(':mb-auth'))
    implementation(project(':mb-autoconfigure'))
    implementation(project(':mb-blog'))
    implementation(project(':mb-chatbot'))
    implementation(project(':mb-graphql'))
    implementation(project(':mb-notification'))
    implementation(project(':mb-restful'))
    
    implementation('org.springframework.boot:spring-boot-starter-web')
}
